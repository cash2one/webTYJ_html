/**
 * Created by NM on 2018/1/18.
 * 车辆管理js
 */

'use strict';

define(function (require) {
    var app = require('../../../../app');

    app.controller('vehicleManagementCtrl', ['$scope', '$http','$rootScope','$location', function ($scope,$http,$rootScope,$location) {

        $scope.doClick(7);
        $scope.btnIndex = 2;
        $scope.doClick = function (index) {
            $scope.btnIndex = index;
        };

    }]);
});