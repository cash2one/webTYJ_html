/**
 * Created by Administrator on 2015/10/29.
 * newContractInitialization
 */

    define(function (require) {
        var app = require('../../../app');

        app.controller('newContractInitializationCtrl', ['$scope', '$http','$rootScope','$location', function ($scope,$http,$rootScope,$location) {
            /**
             * 选中样式
             * @type {number}
             */

            $scope.btnIndex=2;
            $scope.doClick=function(index){
                $scope.btnIndex=index;
            };
        }]);
});