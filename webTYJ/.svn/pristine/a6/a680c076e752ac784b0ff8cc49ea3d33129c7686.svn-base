/**
 * Created by Administrator on 2016-01-25.
 */
define(function (require) {
    var app = require('../../../app');

    app.controller('staffsuperviseITCtrl', ['$scope', '$http','$rootScope','$location', function ($scope,$http,$rootScope,$location) {
        /**
         * 选中样式
         * @type {number}
         */
        $scope.btnIndex = 2;
        $scope.doClick = function(index){
            $scope.btnIndex = index;
        };
    }]);
});
