/**
 * Created by NM on 2018/1/18.
 * 宠物登记js
 */

'use strict';

define(function (require) {
    var app = require('../../../../app');

    app.controller('petCtrl', ['$scope', '$http','$rootScope','$location', function ($scope,$http,$rootScope,$location) {

        $scope.changeShowNum(1);
        $scope.doClick(11);
        $scope.btnIndex = 2;
        //鼠标点击选中
        $scope.doClick=function(index){
            $scope.btnIndex=index;
        };

    }]);
});
